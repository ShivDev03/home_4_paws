import { StyleSheet } from 'react-native';

import { hp, wp } from '@utils/helper';

const style = StyleSheet.create({
  container: { marginHorizontal: wp(10), marginTop: hp(10) },
  imageStyle: { height: hp(18), width: wp(18) },
});

export default style;
